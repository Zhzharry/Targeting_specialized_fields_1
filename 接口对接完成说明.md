# 接口对接完成说明

## 已完成的接口对接

根据接口文档，现在所有接口都已经完成对接：

### ✅ 1. 登录模块 (`/api/login`)

- **POST `/api/login`** - 用户登录 ✓
- **POST `/api/login/register`** - 用户注册 ✓

### ✅ 2. 主页模块 (`/api/home`)

- **GET `/api/home/overview`** - 首页概览 ✓
- **GET `/api/home/me`** - 我的页面信息 ✓
- **GET `/api/home/guess-you-like`** - 猜你喜欢列表 ✓
- **GET `/api/home/go-query`** - 查询页入口推荐 ✓

### ✅ 3. 查询模块 (`/api/query`)

- **GET `/api/query`** - 房源查询 ✓
- **POST `/api/query/favorite`** - 收藏房源 ✓
- **DELETE `/api/query/favorite`** - 取消收藏 ✓
- **POST `/api/query/browse`** - 记录房源浏览 ✓ **（本次新增）**

### ✅ 4. 我的页面模块 (`/api/profile`)

- **POST `/api/profile/preferences`** - 设置偏好 ✓
- **POST `/api/profile/price-predict`** - 房价预测 ✓
- **GET `/api/profile/history`** - 历史记录 ✓
- **GET `/api/profile/favorites`** - 收藏列表 ✓

---

## 本次新增的接口实现

### 1. 添加浏览记录接口 (`/api/query/browse`)

#### 代码位置

- **API 定义**：`frontend/src/api/query.api.ts`
- **调用位置**：
  - `SearchPage.vue` - 搜索页面点击房源
  - `ProfilePage.vue` - 收藏列表和历史记录点击房源

#### API 函数

```typescript
// frontend/src/api/query.api.ts
recordBrowse(userId: number, propertyId: number, source?: 'history' | 'favorite'): Promise<{ message: string; userId: number; propertyId: number }> {
  const params: Record<string, string | number> = { userId, propertyId }
  if (source) {
    params.source = source
  }
  return api.post('/query/browse', null, { params })
}
```

#### 使用场景

##### 场景 1：SearchPage - 搜索结果点击

当用户在搜索页面点击房源卡片时：

- 调用 `viewProperty()` 或 `viewDiscoverProperty()` 函数
- 自动记录浏览，不传 `source` 参数
- 会在数据库中新增浏览记录，并增加房源的 `view_count`

```typescript
// SearchPage.vue
const viewProperty = async (propertyId: number) => {
  // ... 显示房源详情 ...

  // 记录浏览
  try {
    const userId = 1; // 暂时使用固定用户ID
    await queryAPI.recordBrowse(userId, propertyId);
  } catch (error) {
    console.error("记录浏览失败:", error);
  }
};
```

##### 场景 2：ProfilePage - 收藏列表点击

当用户从收藏列表点击房源时：

- 调用 `showPropertyDetailFromFavorite()` 函数
- 传递 `source: 'favorite'` 参数
- 只更新浏览时间，不新增重复记录

```typescript
// ProfilePage.vue
const showPropertyDetailFromFavorite = async (property) => {
  // ... 显示房源详情 ...

  // 记录浏览（从收藏进入）
  if (currentUserId.value && property.propertyId) {
    try {
      await queryAPI.recordBrowse(
        currentUserId.value,
        property.propertyId,
        "favorite"
      );
    } catch (error) {
      console.error("记录浏览失败:", error);
    }
  }
};
```

##### 场景 3：ProfilePage - 浏览历史点击

当用户从浏览历史点击房源时：

- 调用 `showPropertyDetailFromHistory()` 函数
- 传递 `source: 'history'` 参数
- 只更新浏览时间，不新增重复记录

```typescript
// ProfilePage.vue
const showPropertyDetailFromHistory = async (property) => {
  // ... 显示房源详情 ...

  // 记录浏览（从历史记录进入）
  if (currentUserId.value && property.propertyId) {
    try {
      await queryAPI.recordBrowse(
        currentUserId.value,
        property.propertyId,
        "history"
      );
    } catch (error) {
      console.error("记录浏览失败:", error);
    }
  }
};
```

---

## 接口行为说明

### 浏览记录接口 (`POST /api/query/browse`)

#### 请求参数

| 参数         | 类型   | 必填 | 说明                                                                 |
| ------------ | ------ | ---- | -------------------------------------------------------------------- |
| `userId`     | number | 是   | 用户 ID                                                              |
| `propertyId` | number | 是   | 房源 ID                                                              |
| `source`     | string | 否   | 来源标识：`"history"`（从浏览记录进入）或 `"favorite"`（从收藏进入） |

#### 接口行为

1. **无 `source` 参数或普通浏览**：

   - 增加房源的 `view_count` +1
   - 检查是否已有浏览记录
   - 如果有，更新 `created_at` 为当前时间
   - 如果没有，新增浏览记录到 `browsing_history` 表

2. **source = 'favorite'（从收藏进入）**：

   - 增加房源的 `view_count` +1
   - 只更新已有浏览记录的 `created_at`
   - 不新增浏览记录

3. **source = 'history'（从历史记录进入）**：
   - 增加房源的 `view_count` +1
   - 只更新已有浏览记录的 `created_at`
   - 不新增浏览记录

---

## 代码修改清单

### 1. `frontend/src/api/query.api.ts`

- ✅ 新增 `recordBrowse()` 方法

### 2. `frontend/src/views/SearchPage.vue`

- ✅ 修改 `viewProperty()` 函数为 async，添加浏览记录调用
- ✅ 修改 `viewDiscoverProperty()` 函数为 async，添加浏览记录调用

### 3. `frontend/src/views/ProfilePage.vue`

- ✅ 新增 `showPropertyDetailFromFavorite()` 函数（从收藏点击）
- ✅ 新增 `showPropertyDetailFromHistory()` 函数（从历史记录点击）
- ✅ 更新收藏列表模板，使用新的点击处理函数
- ✅ 更新浏览历史模板，使用新的点击处理函数
- ✅ 修改 `PropertyDetail` 接口，添加 `propertyId?` 字段

---

## 测试建议

### 1. 搜索页面浏览测试

- 在搜索页面点击任意房源
- 检查是否正确显示房源详情
- 验证后端 `browsing_history` 表是否新增记录
- 验证 `properties` 表中对应房源的 `view_count` 是否 +1

### 2. 收藏列表浏览测试

- 在个人中心点击"收藏列表"
- 点击收藏的任意房源
- 验证 `source=favorite` 参数是否正确传递
- 验证不会重复新增浏览记录，只更新时间

### 3. 浏览历史浏览测试

- 在个人中心点击"浏览记录"
- 点击历史记录中的任意房源
- 验证 `source=history` 参数是否正确传递
- 验证不会重复新增浏览记录，只更新时间

### 4. 浏览次数统计测试

- 多次点击同一房源
- 验证 `view_count` 是否正确累加
- 验证从不同来源点击是否都能正确统计

---

## 注意事项

1. **用户 ID 管理**：

   - 当前代码中使用了固定的 `userId = 1` 进行测试
   - 生产环境应该从 `authStore` 或 localStorage 获取真实用户 ID

2. **错误处理**：

   - 所有浏览记录调用都使用了 try-catch
   - 失败不会影响用户界面显示
   - 错误会输出到控制台便于调试

3. **性能考虑**：

   - 浏览记录是异步调用，不会阻塞界面
   - 即使接口失败，用户仍可正常查看房源详情

4. **数据一致性**：
   - 后端会确保同一用户对同一房源只有一条浏览记录
   - 重复浏览只更新时间，不会产生重复数据

---

## 总结

现在所有 14 个接口都已完整对接：

- ✅ 登录注册：2 个接口
- ✅ 主页模块：4 个接口
- ✅ 查询模块：4 个接口（包括新增的浏览记录）
- ✅ 个人中心：4 个接口

所有接口都已经在前端实现并正确调用，可以进行完整的功能测试。
