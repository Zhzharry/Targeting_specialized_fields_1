# 其他用户也在看功能说明

## 功能概述

"其他用户也在看"是一个基于 Hadoop MapReduce 计算的智能推荐功能，它会根据用户的浏览和收藏行为，计算用户之间的相似度，然后推荐相似用户喜欢的房源。

## 技术架构

### 后端实现

- **控制器**: `backend/src/main/java/com/example/controller/OthersAlsoViewedController.java`
- **服务**: `HadoopRecommendationService` - 使用 Hadoop MapReduce 计算用户相似度
- **接口路径**: `GET /api/recommendation/others-also-viewed`
- **参数**:
  - `userId` (必填): 当前用户 ID
  - `limit` (可选, 默认 5): 返回的推荐房源数量
  - `excludeViewed` (可选, 默认 true): 是否排除用户已浏览的房源
  - `useCache` (可选, 默认 true): 是否使用 Hadoop 缓存

### 前端实现

- **API 文件**: `frontend/src/api/recommendation.api.ts`
- **类型定义**: `frontend/src/types/api.types.ts`
- **页面组件**: `frontend/src/views/SearchPage.vue`

## 功能特点

### 1. 智能推荐算法

- 使用 Hadoop MapReduce 计算用户相似度
- 基于用户的浏览历史和收藏记录
- 自动排除用户已经浏览过的房源

### 2. 数据来源标识

- **Hadoop 数据**: 显示"基于 Hadoop 计算"徽章
- **数据库数据**: 显示"基于数据库推荐"徽章（降级方案）
- **热门补充**: 当推荐不足时，自动补充热门房源

### 3. 相似度显示

- 每个推荐房源显示与当前用户的相似度百分比
- 相似度通过渐变色徽章展示，视觉效果更佳

### 4. 实时追踪

- 用户点击推荐房源会自动记录浏览行为
- 浏览来源标记为 'recommendation'
- 用于后续的推荐算法优化

## 使用流程

### 1. 用户登录

用户必须先登录才能看到推荐（因为需要根据 userId 进行个性化推荐）

### 2. 浏览房源

用户在搜索页面浏览房源时，系统会记录浏览行为

### 3. 查看推荐

- 在搜索页面的"发现"标签页下
- 位于"猜你喜欢"模块的下方
- 显示 3 个推荐房源（可通过 limit 参数调整）

### 4. 交互操作

- 点击推荐房源卡片查看详情
- 可以收藏推荐的房源
- 系统会记录这些操作用于优化推荐

## 界面设计

### 样式特点

- **渐变卡片**: 使用蓝紫色渐变边框
- **悬浮效果**: 鼠标悬停时卡片上浮并增强阴影
- **数据源徽章**: 顶部显示数据来源的彩色徽章
- **相似度标签**: 底部显示相似度百分比

### 布局结构

```
┌─────────────────────────────────────────┐
│  🔥 其他用户也在看    [基于Hadoop计算]  │
├─────────────────────────────────────────┤
│  ┌────────┐  ┌────────┐  ┌────────┐   │
│  │ 房源1  │  │ 房源2  │  │ 房源3  │   │
│  │ [图片] │  │ [图片] │  │ [图片] │   │
│  │ 标题   │  │ 标题   │  │ 标题   │   │
│  │ 描述   │  │ 描述   │  │ 描述   │   │
│  │ 价格   │  │ 价格   │  │ 价格   │   │
│  │ 标签   │  │ 标签   │  │ 标签   │   │
│  │ 相似85%│  │ 相似92%│  │ 相似78%│   │
│  └────────┘  └────────┘  └────────┘   │
└─────────────────────────────────────────┘
```

## 测试步骤

### 1. 前置条件

- 后端服务运行在 http://localhost:5000
- 前端服务运行在开发模式
- 数据库包含房源数据和用户浏览记录
- Hadoop 服务正常运行（可选，有降级方案）

### 2. 测试流程

1. 启动后端服务

   ```bash
   cd backend
   mvn spring-boot:run
   ```

2. 启动前端服务

   ```bash
   cd frontend
   npm run dev
   ```

3. 登录系统

   - 访问登录页面
   - 输入用户名和密码登录

4. 进入搜索页面

   - 点击顶部导航的"搜索"
   - 切换到"发现"标签页

5. 查看推荐

   - 向下滚动到"其他用户也在看"模块
   - 检查是否显示推荐房源
   - 查看数据源徽章（Hadoop 或数据库）
   - 确认相似度百分比显示

6. 点击推荐
   - 点击任意推荐房源
   - 检查详情弹窗是否正常显示
   - 尝试收藏该房源

### 3. 预期结果

- ✅ 推荐模块正常显示
- ✅ 显示 3 个推荐房源（如果有数据）
- ✅ 每个房源显示相似度百分比
- ✅ 点击房源能正常查看详情
- ✅ 收藏功能正常工作
- ✅ 浏览记录被正确追踪

## 故障排除

### 问题 1: 不显示推荐

**可能原因**:

- 用户未登录
- 数据库中没有足够的浏览记录
- 后端服务未启动

**解决方法**:

1. 检查登录状态
2. 查看浏览器控制台的错误信息
3. 确认后端 API 是否正常响应

### 问题 2: 显示"基于数据库推荐"

**说明**: 这是正常的降级行为

- Hadoop 缓存不可用时会自动降级到数据库查询
- 功能完全正常，只是数据源不同

### 问题 3: 相似度全是 0%

**可能原因**:

- 使用了降级方案（热门房源）
- Hadoop 计算尚未完成

**解决方法**:

- 等待 Hadoop 计算完成
- 增加更多用户浏览数据

## 未来优化方向

### 1. 算法优化

- [ ] 增加更多特征维度（房源属性、价格区间等）
- [ ] 优化相似度计算算法
- [ ] 实现实时推荐更新

### 2. 界面优化

- [ ] 添加"换一批"按钮
- [ ] 支持横向滚动查看更多推荐
- [ ] 添加推荐理由说明

### 3. 性能优化

- [ ] 实现推荐结果缓存
- [ ] 优化 API 响应速度
- [ ] 减少不必要的重复计算

## 相关文件

### 后端

- `backend/src/main/java/com/example/controller/OthersAlsoViewedController.java`
- `backend/src/main/java/com/example/service/hadoop/HadoopRecommendationService.java`

### 前端

- `frontend/src/api/recommendation.api.ts`
- `frontend/src/types/api.types.ts`
- `frontend/src/views/SearchPage.vue`

## 接口文档

### 请求示例

```http
GET /api/recommendation/others-also-viewed?userId=1&limit=3&excludeViewed=true&useCache=true
```

### 响应示例

```json
{
  "items": [
    {
      "propertyId": 123,
      "title": "精装两居室",
      "summary": "阳光小区 · 85㎡ · 2室1厅",
      "score": 0.85,
      "source": "similar_user:456",
      "cover": "http://example.com/image.jpg",
      "totalPrice": 280,
      "tags": ["精装修", "南北通透"]
    }
  ],
  "count": 3,
  "userId": 1,
  "dataSource": "hadoop",
  "lastHadoopUpdate": "2024-01-15T10:30:00",
  "message": "推荐成功"
}
```

## 注意事项

1. **用户隐私**: 推荐算法不会暴露其他用户的个人信息
2. **数据更新**: Hadoop 计算结果会定期更新，不是实时的
3. **降级方案**: 当 Hadoop 不可用时会自动降级到数据库查询
4. **性能影响**: 推荐计算可能需要一定时间，已实现缓存机制

## 更新日志

### 2024-01-15

- ✅ 完成前端 API 集成
- ✅ 实现 SearchPage 组件展示
- ✅ 添加相似度显示
- ✅ 实现点击浏览追踪
- ✅ 添加数据源标识
- ✅ 美化界面样式
