# 🔍 热力图不显示？问题排查指南

## 问题：只有地图，没有热力图

### 快速诊断步骤

#### 1️⃣ 打开浏览器控制台（F12）

查看 Console 标签页，应该看到以下日志：

**✅ 正常情况：**

```
开始加载热力图数据: {city: "beijing", priceRange: "all"}
API响应: {success: true, data: Array(15), ...}
格式化后的数据点数量: 15
示例数据: [{lng: 114.058, lat: 22.535, count: 12}]
✅ 热力图数据已设置
```

**❌ 问题情况：**

```
⚠️ API返回数据为空或失败
// 或
❌ 加载热力图数据失败: ...
// 或
⚠️ 热力图层未初始化
```

---

### 常见问题及解决方案

#### 问题 1: API 返回空数据

**症状：** 控制台显示 "⚠️ API 返回数据为空或失败"

**解决方法：**

1. **检查数据库是否有数据**

   ```sql
   -- 执行这个SQL检查
   SELECT COUNT(*) FROM properties p
   INNER JOIN communities c ON p.community_id = c.community_id
   WHERE JSON_EXTRACT(c.location_info, '$.latitude') IS NOT NULL;
   ```

   如果返回 0，说明没有经纬度数据，需要插入测试数据：

   ```bash
   mysql -u root -p bigdata < backend/插入热力图测试数据.sql
   ```

2. **检查后端服务是否启动**
   - 访问：http://localhost:8080/api/heatmap/data?city=beijing&priceRange=all
   - 应该看到 JSON 响应，包含数据点数组

---

#### 问题 2: 热力图层未初始化

**症状：** 控制台显示 "⚠️ 热力图层未初始化"

**解决方法：**

1. **检查高德地图 Key 是否配置**
   - 打开 `frontend/src/views/ToolsPage.vue`
   - 搜索 `key:` 确认不是 `YOUR_AMAP_KEY`
2. **确保切换到热力图标签**

   - 地图只在点击"🗺️ 价格热力图"标签后才初始化

3. **检查浏览器控制台的 Network 标签**
   - 看是否成功加载高德地图脚本
   - 查找 `webapi.amap.com` 的请求

---

#### 问题 3: 后端连接失败

**症状：** 控制台显示网络错误或 404

**解决方法：**

1. **确认后端已启动**

   ```bash
   cd backend
   mvn spring-boot:run
   ```

2. **检查端口**

   - 后端应该在 http://localhost:8080
   - 前端在 http://localhost:5173

3. **检查 CORS 配置**
   - HeatmapController 应该有 `@CrossOrigin(origins = "http://localhost:5173")`

---

#### 问题 4: 热力图数据格式错误

**症状：** 控制台显示 "没有有效的数据点"

**解决方法：**

查看 API 返回的数据结构：

```javascript
// 正确格式
{
  success: true,
  data: [
    {
      longitude: 114.058,  // 必须是数字
      latitude: 22.535,    // 必须是数字
      price: 120000,       // 必须是数字
      propertyName: "xx小区",
      district: "福田区"
    }
  ]
}
```

如果经纬度或价格是 null 或字符串，需要检查后端代码。

---

### 🔧 一键修复脚本

如果上述方法都不行，执行以下步骤：

#### Step 1: 插入测试数据

```bash
cd backend
mysql -u root -p bigdata < 插入热力图测试数据.sql
```

#### Step 2: 重启后端

```bash
cd backend
mvn clean package
mvn spring-boot:run
```

#### Step 3: 清空浏览器缓存

- Chrome: Ctrl + Shift + Delete
- 选择"缓存的图片和文件"
- 清除数据

#### Step 4: 刷新前端

- 按 F5 或 Ctrl + R 刷新页面
- 重新点击"🗺️ 价格热力图"标签

---

### 📊 验证成功

成功后，你应该看到：

1. **地图上有彩色的热力区域**

   - 红色 = 高价区域
   - 黄色 = 中等价位
   - 绿色 = 低价区域

2. **底部显示统计信息**

   - 房源总数
   - 平均单价
   - 最低单价
   - 最高单价

3. **可以进行交互**
   - 切换城市
   - 筛选价格范围
   - 调节透明度

---

### 🆘 还是不行？

#### 检查完整日志：

**前端（浏览器控制台）：**

```
F12 → Console → 查看所有红色错误信息
```

**后端（IDEA/命令行）：**

```
查看 Spring Boot 启动日志
搜索 "热力图" 关键词
```

#### 手动测试 API：

1. 浏览器访问：

   ```
   http://localhost:8080/api/heatmap/data?city=beijing&priceRange=all
   ```

2. 应该看到 JSON 响应：

   ```json
   {
     "success": true,
     "data": [...],
     "total": 15,
     "message": "数据加载成功"
   }
   ```

3. 如果看到 `"data": []`，说明数据库没有数据，执行测试数据脚本

---

### 📞 需要帮助？

提供以下信息：

1. 浏览器控制台的完整错误信息
2. 后端启动日志
3. `http://localhost:8080/api/heatmap/data` 的返回结果
4. 数据库查询结果（测试 SQL）
