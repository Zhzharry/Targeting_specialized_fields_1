# 🔍 热力图显示诊断指南

## 当前状态：地图显示了，但没有热力图

### 关于 securityJsCode (安全密钥)

**Q: 是否需要配置安全密钥？**

A: 这取决于你在高德开放平台的配置：

#### 检查方法：

1. 登录 [高德开放平台](https://console.amap.com/dev/key/app)
2. 找到你的应用
3. 查看 "Web 端(JS API)" 的密钥配置

**情况 1: 未设置安全密钥** ✅ 推荐

- 平台显示：安全密钥未设置
- **不需要** 在代码中配置 `securityJsCode`
- 地图可以正常显示

**情况 2: 已设置安全密钥** ⚠️

- 平台显示：已设置安全密钥（一串代码）
- **必须** 在代码中配置 `securityJsCode`
- 否则地图 API 调用会受限

#### 如果需要配置安全密钥：

在 `ToolsPage.vue` 中，找到：

```typescript
const AMap = await AMapLoader.load({
  key: "cc670ed2985b867a48128c2fdd4f1ced",
  version: "2.0",
  plugins: ["AMap.HeatMap"],
  // securityJsCode: 'YOUR_SECURITY_CODE', // 取消注释这行
});
```

改为：

```typescript
const AMap = await AMapLoader.load({
  key: "cc670ed2985b867a48128c2fdd4f1ced",
  version: "2.0",
  plugins: ["AMap.HeatMap"],
  securityJsCode: "你的安全密钥", // 从平台复制
});
```

---

## 当前问题排查

### 步骤 1: 检查控制台完整输出

打开浏览器控制台（F12），应该看到：

```
开始初始化高德地图...
加载高德地图 API...
高德地图 API 加载成功
创建地图实例...
地图实例创建成功，加载热力图插件...
热力图插件加载成功，创建热力图层...
热力图层创建成功，层对象: [Object]
开始加载数据...
开始加载热力图数据: {city: "beijing", priceRange: "all"}
API响应: {success: true, data: Array(15), ...}
格式化后的数据点数量: 15
示例数据: [{lng: ..., lat: ..., count: ...}]
准备设置热力图数据，数据集: {count: 15, sample: [...], max: 20}
✅ 热力图数据已设置并显示
```

### 步骤 2: 检查数据是否返回

在控制台输入并执行：

```javascript
fetch("http://localhost:8080/api/heatmap/data?city=beijing&priceRange=all")
  .then((r) => r.json())
  .then((d) => console.log("API数据:", d));
```

**正常返回：**

```json
{
  "success": true,
  "data": [
    {
      "longitude": 113.940,
      "latitude": 22.541,
      "price": 95000,
      "propertyName": "南山科技园 精装三房",
      "district": "南山区"
    },
    ...
  ],
  "total": 15
}
```

### 步骤 3: 手动显示热力图

如果数据正常但热力图不显示，在控制台执行：

```javascript
// 检查热力图层
console.log("热力图层:", window.heatmapLayer);

// 手动显示
if (window.heatmapLayer) {
  window.heatmapLayer.show();
  console.log("已手动显示热力图");
}
```

### 步骤 4: 调整地图视角

热力图可能因为地图缩放级别或位置看不到，尝试：

```javascript
// 调整缩放级别
if (window.amapInstance) {
  window.amapInstance.setZoom(12); // 放大
  console.log("已调整缩放级别");
}
```

---

## 常见问题

### 问题 1: 热力图颜色太淡看不见

**解决方法 1:** 增加透明度

```javascript
if (window.heatmapLayer) {
  window.heatmapLayer.setOptions({
    opacity: [0, 1.0], // 最大不透明度
  });
}
```

**解决方法 2:** 增加半径

```javascript
if (window.heatmapLayer) {
  window.heatmapLayer.setOptions({
    radius: 50, // 增大热力半径
  });
}
```

### 问题 2: 数据点太少看不出效果

**检查数据量：**

```sql
SELECT COUNT(*) FROM properties p
INNER JOIN communities c ON p.community_id = c.community_id
WHERE JSON_EXTRACT(c.location_info, '$.latitude') IS NOT NULL;
```

如果少于 10 条，执行：

```bash
mysql -u root -p bigdata < backend/插入热力图测试数据.sql
```

### 问题 3: 数据坐标不在当前视野

**确认坐标范围：**

```sql
SELECT
  MIN(CAST(JSON_UNQUOTE(JSON_EXTRACT(location_info, '$.latitude')) AS DECIMAL)) as min_lat,
  MAX(CAST(JSON_UNQUOTE(JSON_EXTRACT(location_info, '$.latitude')) AS DECIMAL)) as max_lat,
  MIN(CAST(JSON_UNQUOTE(JSON_EXTRACT(location_info, '$.longitude')) AS DECIMAL)) as min_lng,
  MAX(CAST(JSON_UNQUOTE(JSON_EXTRACT(location_info, '$.longitude')) AS DECIMAL)) as max_lng
FROM communities
WHERE JSON_EXTRACT(location_info, '$.latitude') IS NOT NULL;
```

**深圳地区应该是：**

- 纬度: 22.5 - 22.6
- 经度: 113.9 - 114.1

---

## 快速测试脚本

在浏览器控制台粘贴并执行：

```javascript
// 完整诊断脚本
(function () {
  console.log("=== 热力图诊断开始 ===");

  // 检查地图实例
  if (window.amapInstance) {
    console.log("✅ 地图实例存在");
    console.log("地图中心:", window.amapInstance.getCenter());
    console.log("缩放级别:", window.amapInstance.getZoom());
  } else {
    console.log("❌ 地图实例不存在");
  }

  // 检查热力图层
  if (window.heatmapLayer) {
    console.log("✅ 热力图层存在");
    // 强制显示
    window.heatmapLayer.show();
    console.log("已调用 show()");
  } else {
    console.log("❌ 热力图层不存在");
  }

  // 测试API
  fetch("http://localhost:8080/api/heatmap/data?city=beijing&priceRange=all")
    .then((r) => r.json())
    .then((data) => {
      console.log("✅ API响应:", data);
      if (data.success && data.data && data.data.length > 0) {
        console.log("数据点数量:", data.data.length);
        console.log("第一个数据点:", data.data[0]);
      } else {
        console.log("❌ API无数据");
      }
    })
    .catch((e) => console.log("❌ API请求失败:", e));

  console.log("=== 诊断完成 ===");
})();
```

---

## 如果还是不显示

1. **完全刷新页面**（Ctrl + Shift + R）
2. **清除浏览器缓存**
3. **切换到深圳**（selectedCity = 'shenzhen'）因为测试数据在深圳
4. **调整透明度滑块**到 100%
5. **重启后端服务**
6. **检查是否有 JS 报错**（控制台红色错误）

---

## 成功标志

当热力图正常显示时，你会看到：

1. ✅ 地图上有**彩色的渐变区域**
2. ✅ 红色区域表示高价位（福田中心区）
3. ✅ 黄色区域表示中等价位（南山、后海）
4. ✅ 蓝绿色区域表示低价位（罗湖、其他）
5. ✅ 底部统计数据显示正常（房源总数、平均单价等）
